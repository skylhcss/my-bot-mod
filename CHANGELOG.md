# 更新日志 (Changelog)

本文档记录 My Bot Mod 的所有版本更新历史。

**最后更新**: 2026-08-20

---

## [1.3.4] - 2026-08-20

### 📦 发布修复
- **行为编辑器随 JAR 发布**：修复 v1.3.3 发布包未附带行为编辑器的问题——编辑器（HTML + 离线 Blockly 资源 + 示例）打包进全部 7 个版本 JAR 的 `assets/my-bot-mod/editor/`，游戏启动时自动释放到 `config/my-bot-mod/editor/`（文件缺失或大小变化才写入，升级后自动更新，失败仅告警不阻断加载）；发布只需上传 JAR 即附带编辑器，行为管理界面提示与 README / BEHAVIORS / DEVELOPMENT 文档同步指向新位置

---

## [1.3.3] - 2026-08-17

### ✨ 新功能
- **游戏内行为编辑器**：行为管理界面"新建行为"即可在游戏内编写行为（分类面板 + 语句列表 + 参数表单，支持嵌套控制流逐层编辑与面包屑导航），产物与 HTML 编辑器完全兼容
- **行为导出**：编辑器"保存"自动写入 config/my-bot-mod/behaviors/ 并立即重载；可在"导出目录"指定任意文件夹导出副本（服务端校验，拒绝 ".." 路径段）
- **新外置输出积木**：`outputRaw` 原始行输出（无前缀）、`outputTemplate` 自定义模板输出（`{time}` `{bot}` `{var:名}` 占位符）、`outputTable` 自定义表格输出（自定义表头 + 逐行追加 CSV）；现有输出积木新增 追加/覆盖 写入模式
- **行为执行界面重构**：面板加大，顶部状态行实时显示正在执行的行为与队列进度（第 x/y 个）；启动/停止按钮分离；播放列表 ▲▼ 双向排序/移除/一键清空；可用行为搜索框 + 每行"▶ 只跑这一个"快捷执行（不改动播放列表）与 ✎ 编辑入口
- **创造飞行继承**：创造/旁观模式飞行中召唤的假人同样进入飞行状态并空中悬停（跳跃/潜行 上升/下降，落地自动结束飞行，与原版创造飞行一致；飞行速度使用假人自身默认值）；飞行状态随 preserveBotState 驻留保存
- **末影箱菜单改进**：`/bot <名字> enderchest` 直接绑定假人末影箱实时编辑，关闭时自动保存
- **支持 Minecraft 1.20.5–1.21.4**：新增构建目标 `1.20.6`（覆盖 1.20.5–1.20.6）、`1.21.1`（覆盖 1.21–1.21.1）、`1.21.3`（覆盖 1.21.2–1.21.3）、`1.21.4`，共 7 个版本 JAR；单代码库经 Stonecutter 条件注释适配各 API 断层（Fabric 网络改 CustomPacketPayload、扩展菜单改 StreamCodec、ItemStack/效果序列化组件化、Entity.hurt→hurtServer、跨维度 teleportTo 新签名、HudRenderCallback/UseItemCallback 签名变更等）；新版本 Java 编译目标 21（1.20.1–1.20.4 保持 17），fabric.mod.json 的 java 依赖按版本自动注入；构建工具链升级 Gradle 9.7 + Fabric Loom 1.17.19
- **智能路径平滑**（`pathfindingSmooth`，默认开）：宽度感知视线法合并可直行的路标点，走位更顺。仅合并同高度、中心线与 ±0.3 侧向净空均可站立的线段，跳跃/下落/游泳锚点与裂谷不受影响；采样间隔 0.25 格，复用 A* 同一套可站立判定避免偏航/卡角

### 🔧 寻路调整与修复
- **寻路器重构为同步优化 A\***：移除分帧增量机制（时间预算/跨 tick 搜索状态），发起寻路时一次性完成搜索；保留并强化优化手段——fastutil 免装箱哈希结构、懒删除、方块状态缓存（未加载区块按基岩）、可采纳启发式、迭代/节点硬上限（15000 次/25000 节点）
- **移除旧路径平滑**（String-pulling）：其采样与可占据判定不一致导致偏航/卡角，已由上述宽度感知智能平滑（可配置）取代
- 修复禁用游泳路线时下落扫描仍会把水面当落点，导致路径引入水中节点
- 修复飞行/高处假人无法开始寻路（起点规范化向下扫描深度 3 → 24 格）
- 修复自由落体期间被误判为卡住（空中不计卡住计数，避免长下落触发无效重算）
- 修复卡住重算反复回到最初路线而卡死：A* 是确定性的，而禁区仅在连续卡住 ≥2 次时添加、且假人一移动连续计数就归零，导致后续重算无新禁区而算回原路线 → 改为每次卡住重算都累加禁区，另以禁区总量上限兼顾放弃判定
- 修复假人游泳一段后停止划水下沉：原划水条件依赖眼睛是否在水中，水面漂浮时眼睛出水即停止划水 → 改为目标不低于当前位置就持续划水保持浮力，仅目标在下方时下潜
- 修复游戏内行为编辑器部分元素超出背景框：调色板/语句列表底部裁剪改为“整行完全落在区域内”才渲染，嵌套语句行按右侧按钮真实宽度预留（修正嵌套体按钮宽度少算），顶栏描述框窄窗口下自适应收缩

### 🗑️ 功能移除
- **完全移除编组与合作建造系统**：`/botmod group *` 与 `/bot <名字> build *` 命令、编组/建造/原理图相关界面、指挥棒建造模式、行为积木（build*/group*/补给传感器）与相关配置项（enableBuildFeature 等）全部移除
- 移除从未实现的 `onMessage` 消息匹配事件（解析器接受但运行时静默忽略，属死功能）
- 移除无效配置项 `configMenuKey`（客户端硬编码 B 键，改它无效；改键请用原版控制设置）
- **移除 Carpet 兼容模式**（`carpetModCompatibility` 配置与自动检测禁用逻辑）：可与 Carpet 同时安装，各自管理自己的假人
- **移除指挥棒传送的创造模式限制**（`allowBatonTeleportNonCreative` 配置）：传送模式任意游戏模式均可使用（OP/`batonRequiresOp` 权限校验不变）

### 🐞 BUG 修复
- 修复**右键面板“删除假人”按钮永远无效**（GUI 确认后现走新增的 `/bot <名字> kill confirm` 直接执行）
- **补全实现**行为系统中仅有声明的积木：`followEntity`/`stopFollow`（持续看向跟随）与 9 个传感器（holding/hasItem/fishHooked/usingItem/blockBelow/facing/nearestEntityCoord/nearestEntityDistance/nearestEntityExists）
- 行为脚本条件求值/循环帧异常不再冒泡崩掉服务器 tick（记录错误并继续）
- 修复 `broadcast` 语句导致自身事件体在同 tick 被执行两次（改为 tick 末尾分发）
- 行为重复启动不再泄漏旧运行时；假人删除时关闭其残留的容器打开状态
- `createBot` 中途异常时回滚已注册的假玩家，不再残留无法清除的“幽灵玩家”
- 修复假人自动重生后其他玩家客户端永久躺尸（重生后重建客户端实体，含 1.20.2+ 适配）
- 攻击/右键交互的实体射线现在被方块遮挡截断，不再隔墙命中
- 假人背包界面新增同维度与距离校验，不能跨维度/无限距离操作
- 驻留恢复不再把每玩家限额记在加入者头上（恢复属重建，旁路该限额）；死亡中的假人不保存，恢复血量钳下界
- `/botmod config set/get` 补齐 7 个缺失配置项（batonRequiresOp/botGlowing/botFireImmune/maxBotsPerPlayer/maxPathfindingDistance/pathfindingAllowParkour/pathfindingAllowSwim），数值参数范围与 validate 对齐
- 皮肤文件名防路径穿越校验；皮肤缓存改并发安全集合
- 指挥棒传送提示仅在单机显示（联机时服务端配置为准，客户端不再误报）
- 清理 3 个孤儿翻译键，文档同步精简更新

---

## [1.3.3-beta] - 2026-07-19（预览版）

### 🧵 行为系统 v2
- **Scratch 式多脚本执行模型**：每个帽子块一个独立线程（并联），链内串联；挂起状态按线程隔离
- **每假人专属行为管理界面**（右键面板→行为管理）：实时自动刷新，不再藏在全局配置菜单里
- 积木扩容至 **120+/13 分类**：新增列表（12 块，1-based）、文本处理（8 块）、三角/常量、新事件（广播/血量/实体接近）、等待直到、跨假人广播、装备/丢弃指定物品、看向实体、方块/天气/玩家等 15 个新传感器
- 移除语义模糊的"循环"开关（由"永远重复"积木与事件驻留承担）；示例重做（巡逻问候含聊天回应、盘点助手演示列表）
- CI 修复：GitHub Actions JDK 17→21（Loom 1.16 要求 Gradle 运行在 JVM 21+）

---

## [1.3.3-alpha] - 2026-07-19（预览版）

### 🧩 假人行为系统（动作管理 + 外置输出 + Blockly 编辑器）
- **行为脚本运行时**（`name.modid.behavior`）：完整 Scratch 式解释器——变量/四则与逻辑表达式/比较/拼接、重复 N 次/当…重复/如果否则/永远重复、wait 与寻路挂起语义；每 tick 执行预算，无等待死循环不卡服
- **新动作**：聊天说话（系统消息安全模拟）、看向坐标、读取容器内容到变量、导出容器/背包清单、丰富传感器（血量/饥饿/坐标/维度/手持/背包计数/周围生物/容器计数/时间/随机数）
- **外置输出**：行为可将内容写入 `my-bot-mod-exports/`，支持 txt / JSON Lines / CSV 三格式，文件名防穿越、IO 异步
- **行为管理页**（B 键 → 行为）：选择假人、行为播放列表（多行为顺序执行、上移/移除）、启动/停止、重扫文件夹、解析错误展示；播放列表随驻留保存
- **Blockly 图形化行为编辑器**（`editor/behavior-editor.html`，离线可用）：类 Scratch 拖拽积木，40+ 积木分六大彩色分类，深色主题、中英切换、自动草稿、保存校验与错误高亮；行为文件内嵌工作区快照可无损回读
- **命令**：`/bot <名字> behavior list|assign|unassign|start|stop|reload`（含 tab 补全）；手动 stop/指挥棒下令自动暂停行为脚本
- 假人面板新增行为运行状态行；示例行为见 `editor/examples/`；新增文档 docs/BEHAVIORS.md

### ⚡ 事件积木（Scratch 式帽子块）
- **▶ 当行为启动时** / **💬 当玩家发送"…"时**：聊天包含匹配（留空=任意），发送者/内容注入 `chat_sender`/`chat_message` 变量；触发时打断当前动作改跑触发体；纯事件脚本驻留监听不结束

### 📦 容器拟真交互
- 新积木：打开/关闭容器、**从容器取出/放入物品**（实际数量存变量）+ 4 个容器传感器（格子数/指定格物品/指定格数量/物品总数）
- **拟真流程**：够不着先寻路到附近 → 旋转视角看向容器 → 挥手 → 真实 openMenu 打开（箱盖动画/声音/viewer 计数生效）→ 操作；换容器/行为结束/手动停止自动关闭

### 🎨 编辑器 v2（现代化 UI）
- 渐变顶栏 + SVG 图标按钮（撤销/重做/居中/新建/打开/保存）、iOS 式循环开关、积木计数徽章
- **回收站修复**：Blockly media 资源本地化（之前从网络加载导致图标缺失），删除的积木可从回收站找回
- 积木扩容至 **60+（11 分类）**：MC 物品选择器（90+ 常用物品）、实体选择器（40 实体）、乘方/平方根/绝对值/取整/较大较小值、Blockly 自定义函数（可带参数，递归深度保护）、周围生物按类型过滤

---

## [1.3.2] - 2026-07-19

### 🧱 多版本支持（1.20–1.20.4）
- 接入 **Stonecutter** 单代码库多版本构建：构建目标 `1.20.1`（覆盖 1.20–1.20.1）、`1.20.2`、`1.20.4`（覆盖 1.20.3–1.20.4），`gradle chiseledBuild` 一键产出全部 JAR（产物名带 `+<mc>` 后缀）
- 1.20.2+ 适配：假人注册链路（`ClientInformation`/`CommonListenerCookie`）、common 协议包监听器、`SavedData.Factory` 驻留存储、`PlayerSkin` record 皮肤 Mixin、`renderBackground` 新签名
- CI 改为全版本构建并上传各版本产物
- Modrinth 按 MC 版本拆分发布 JAR（`1.3.2+1.20.1 / 1.20.2 / 1.20.4`）

### 🖥️ 配置界面全面重构（ModernConfigScreen）
- **浏览器式标签页**：分类以标签页打开，可单独关闭（✕）、点击切换，重开界面恢复上次打开的标签
- **工作台风格主页**：灰色工作台面板 + 3×3 凹槽九宫格，每个分类一个物品图标槽位，点击即开
- **优雅搜索框**：标签页右端内嵌搜索框（深底、聚焦青蓝描边），**中英文均可实时过滤**当前页配置项
- **新增"寻路"分类页**（指南针图标）；General 页新增"OP/权限"分区
- 青蓝强调色、紧凑字体与行高；假人设置面板同步紧凑化并新增**状态行**（空闲/战斗/寻路 + 饥饿值），移除面板内手持槽位切换（背包界面仍可设置）

### ⚙️ 新配置项
- **OP/权限**：`maxBotsPerPlayer`（每玩家假人上限，0=无限）、`batonRequiresOp`（指挥棒需 OP）
- **寻路**：`maxPathfindingDistance`（32-1024，默认 256）、`pathfindingAllowParkour`、`pathfindingAllowSwim`
- **外观/生存**：`botGlowing`（假人发光）、`botFireImmune`（免疫火焰/岩浆）
- **假人个人配置**新增两项三态覆盖：**发光**、**免疫火焰**（共 7 项，优先于全局，随驻留保存）

### 🗺️ 寻路增强
- **禁止跨维度寻路**：指挥棒对不同维度的假人下达寻路指令会被直接拒绝（传送仍支持跨维度）
- 多假人**共享每 tick 计算预算**，大量假人同时寻路不再卡服
- **卡住自动绕行/放弃**：连续卡住 2 次将当前位置加入禁区绕行，4 次或重算无果自动放弃
- 栅栏/围墙（1.5 格高）不可跳越判定、跳跃头顶空间检查（不再对 2 格高障碍原地跳）
- 攀爬藤蔓/梯子时停止水平输入（不再前后乱动掉落）；路径终点停止移动（无残留漂移）；重算后不再回头
- 寻路瞬态输入不写入驻留数据（修复退出重进后假人一直向前走）

### 🎮 动作控制（对齐 Carpet）
- `use once` 对弓/食物等按住类物品会正确**触发并释放**（不再拉弓不放）
- `use continuous` 不再每 tick 重触发按住类物品；`use interval` 间隙释放；`stop`/停止使用时释放使用中物品

### 🎨 皮肤系统
- 回归稳定的**同步获取**实现：修复"只能获取到 Mojang 默认皮肤"与 `Bad signature length` 报错；离线服务器可正常按正版名取皮肤

### 🌏 全面双语化（i18n）
- 所有服务端消息与客户端界面文本完成中英双语（en_us/zh_cn 220 键完全对齐）；README 双语化

### 🛡️ 破坏性命令二次确认
- `/bot <name> kill` 与 `/botmod config reset` 需在 **15 秒内再次执行**确认，防止误删/误重置

### 🐛 其他修复与清理
- 驻留数据迁移为**纯 NBT** 读写（弃用 GSON/SNBT 中间层）
- 假人加入/离开不再触发多余的玩家 JOIN 处理；协议版本校验；假人列表增量广播
- 删除 `/bot test` 测试命令与冗余文档；接入 Checkstyle（仅报告）

---

## [1.3.1] - 2026-07-19

### ✨ 新功能
- **指挥棒（Command Baton）**：两根木棍合成、不可堆叠的手持道具
  - 手持时屏幕**左上**显示模式/操作/选中假人信息（血量、距离、坐标、维度、状态），**右上**显示假人列表
  - `Ctrl + 滚轮` 切换模式（指挥寻路 / 传送）
  - `Alt + 滚轮` 或 `Alt + 右键看向假人` 选择假人（任意距离）
  - `右键看向某处` 让选中假人**寻路**前往或**传送**至该处（沿准星射线，最远 160 格）
  - 传送模式默认仅手持玩家为创造模式时可用，配置 `allowBatonTeleportNonCreative` 可放开
  - 传送/寻路均支持**跨维度**（传送直接跨维度；寻路先把假人拉到指挥者维度再寻路）

### 🗺️ 寻路系统全面重构（BotPathfinder）
- **非阻塞分帧 A***：跨 tick 计算（每 tick 迭代预算），长距离寻路不再卡服；重算时保留旧路径继续行走
- **游泳**：可下水、水面穿行与上岸；水下也能开始寻路，游动时自动浮出水面避免溺水
- **跨越裂谷 / 跑酷**：可助跑起跳跨越 1-3 格裂谷，滞空时保持移动；落入水中无摔落伤害
- **危险规避**：不在岩浆/火/仙人掌/岩浆块/细雪等致命方块上落脚
- **修正到达判定**：走到目标方块本身而非停在旁边；启发式修正；路径平滑（string-pulling）
- 方块状态缓存减少世界访问；重算加入冷却避免每 tick 重算

### 🐛 Bug 修复
- ✅ 修复**生存模式挖矿永远挖不动**（每 tick 重发 `START_DESTROY_BLOCK` 会重置进度；改为每方块仅发一次、切换/停止发 `ABORT`）
- ✅ 修复配置文件损坏时抛 `JsonSyntaxException` 导致崩溃（兼捕 `JsonParseException` 并回退默认）
- ✅ 修复**驻留区块票据 300tick 过期**导致静止假人区块被卸载（无条件刷新），以及跨维度票据泄漏/漏加（票据记录所属维度）
- ✅ 修复死亡自动重生忽略重生点维度（跨维度死亡传送到错误位置）
- ✅ 修复假人未注册进 `playersByUUID`（改用 `placeNewPlayer` 规范注册/移除，`getPlayer(UUID)`、计分板等恢复正常）
- ✅ 修复 PNG 皮肤加载失败时每帧重复 IO 与刷屏日志（负缓存）
- ✅ 修复快捷键绑定为方向键/退格/删除时静默失效，以及“先按主键后补修饰键”的组合键无法触发
- ✅ 修复挖掘建筑高度硬编码 320（改用当前维度 `getMaxBuildHeight()`）
- ✅ 其他：`getInstance()` 线程安全、元数据字段不再被持久化覆盖、白名单误删/滚轮吞事件、滑块除零等

### ⚙️ 配置变更
- 新增 `allowBatonTeleportNonCreative`（布尔，默认 false）：指挥棒传送模式是否允许非创造模式
- `allowNonOpCreateBot` **更名为** `allowNonOpControlBot`（语义为“创建并控制”；旧配置文件自动迁移）

### 📝 文档
- 全面更新并精简 README 与 docs/（命令、配置、皮肤、开发、FAQ）

---

## [1.3.0] - 2026-07-18

### ✨ 新功能
- **假人背包界面**（原版风格，可编辑）
  - 对着假人**右键**打开设置面板，面板内点击"打开背包"
  - 完整展示主物品栏 36 格、盔甲 4 格、副手 1 格，可自由放入/取出/整理，改动实时同步到假人
  - 界面左侧渲染假人 3D 模型（跟随鼠标转动）
  - 右上 3x3 选择板可设置假人**当前手持槽位**（0-8），当前手持格以绿色高亮
  - 命令入口：`/bot <name> inventory`
- **假人末影箱界面**（原版三行箱子，可编辑）
  - 面板内点击"打开末影箱"，或命令 `/bot <name> enderchest`
- **每个假人独立设置面板**（原版风格）
  - 对着假人右键打开，显示名字、维度、坐标、生命、游戏模式
  - 快捷入口：打开背包 / 打开末影箱
  - 每假人设置：游戏模式切换、手持槽位选择
  - 常用控制：停止动作、传送到我、删除假人
- **新增命令**：`inventory`、`enderchest`、`panel`、`slot <0-8>`、`gamemode <mode>`、`tphere`

### 🐛 Bug 修复
- ✅ 修复召唤假人时同步请求 Mojang 皮肤 API **阻塞服务器线程**（改为后台异步获取，成功后刷新客户端显示）
- ✅ 修复"死亡自动重生"不可靠：仅重置血量无法真正复活（死亡计时到 20 tick 后实体被移除）。现在正确重置 `dead`/`deathTime`/血量/饱食/效果/着火，并在移除阀值前复活
- ✅ 修复自动重生重生点为空时不处理（回退到创建者位置）
- ✅ 修复 `/bot` 命令权限判断使用过期配置实例（`/botmod config reload|reset` 后失效）——改为实时读取配置
- ✅ 修复客户端 PNG 皮肤动态纹理泄漏（假人移除时释放 GPU 纹理，断开连接时清空缓存）
- ✅ 规范异常处理：`createBot` 等改用日志记录替代 `printStackTrace`
- ✅ 修复假人背包界面过高溢出屏幕（改为左右分布：左列装备/模型/假人物品栏，右列查看者物品栏）
- ✅ 修复假人主物品栏不驻留（此前存为 list SNBT 却按 compound 解析导致丢失；改为 `{Items:[...]}` 包裹保存/读取）
- ✅ 修复空物品栏假人退出重进时出现的虚假报错"缺少'{'"（兼容旧版 list 存档格式）

### 🔧 技术改进
- 新增自定义容器菜单 `BotInventoryMenu`（`ExtendedScreenHandlerType`，向客户端传递假人 UUID 与手持槽位）
- 新增轻量网络通道 `BotNetworking`（S2C `open_bot_panel`）+ `UseEntityCallback` 右键交互
- 皮肤缓存改用并发容器，支持后台线程安全写入
- 驻留数据新增手持槽位（`selectedSlot`）的保存与恢复

### 🎨 界面增强
- 假人设置面板改为全局配置界面（深色）风格：左侧为操作（背包/末影箱/游戏模式/手持槽位/停止/传送/删除），右侧为假人**个人配置**
- 新增假人**个人配置**（三态：继承/强制开/强制关，优先于全局）：受到伤害、会饥饿、死亡自动重生、自动跳跃、杀戮光环（随假人驻留保存）
- 全局配置界面支持折叠分类，新增"假人"标签页（列出所有假人，点击打开其设置面板）
- 切换手持槽位不再弹出聊天提示

### 📝 文档更新
- README/COMMANDS/CONFIG/FAQ/DEVELOPMENT 全面更新背包、末影箱、设置面板与右键打开方式
- 修复 README 中重复的更新日志条目

---

## [1.2.1a] - 2026-06-20

### ✨ 新功能
- **假人自动跳跃** (`allowBotAutoJump`)
  - 启用后假人在移动时遇到1格高障碍物自动跳跃
  - 使用 Minecraft 内置 `horizontalCollision` 碰撞检测，与玩家跳跃行为完全一致
  - 可通过配置界面、命令或配置文件控制
  - 默认启用

- **goto 寻路命令** (`/bot <name> goto <x> <y> <z>`)
  - 基于 A* 算法的全新寻路系统（借鉴 Baritone 设计）
  - 所有路径节点保证为有效"站立位置"（脚踩固体 + 头部空间）
  - 支持跳跃1格高障碍、下落1-3格、水平8方向移动
  - 无距离硬限制，支持远距离寻路（部分路径回退机制）
  - 自动重新计算路径（每100 tick）
  - 卡住自动重寻路（30 tick 无水平移动后触发）
  - `/bot <name> goto stop` 取消寻路

### 🐛 Bug 修复
- ✅ 修复 killAura 无攻击冷却导致伤害异常（添加 `getAttackStrengthScale` 检查）
- ✅ 修复 killAura 误伤友方实体（排除其他假人和创建者）
- ✅ 修复生存模式方块瞬间破坏（改用 `handleBlockBreakAction` 渐进式挖掘）
- ✅ 修复 BotPersistenceManager 使用反射访问私有字段（改用 public getter）
- ✅ 修复区块加载票据重复添加（`loadAllBotsForPlayer` 中移除冗余调用）
- ✅ 修复假人死亡后自动重生 TickTask 无安全检查（添加 `isRemoved()` 保护）
- ✅ 修复 NPE 被静默吞掉（改为记录日志）
- ✅ 修复 BotSkinTextureLoader GPU 纹理未释放（`clearCache()` 和 `removeCache()` 调用 `release()`）
- ✅ 修复 `worldLoadedFlags` 静态 Map 内存泄漏（服务器关闭时 `clearAllLoadedFlags()`）
- ✅ 修复 ModConfig 空白名单无法保存（改为仅在 `null` 时初始化默认值）
- ✅ 修复 BotSkinManager 重复阻塞 Mojang API 请求（添加负面缓存）
- ✅ 修复快捷键组合键误触发（修饰键精确匹配 + 边沿检测）
- ✅ 修复快捷键重映射后旧键仍生效（统一使用 GLFW 直接检测）
- ✅ 修复假人自动跳跃不生效（改用 `horizontalCollision` 碰撞检测）
- ✅ 修复寻路时被头顶方块卡住（`moveToWaypoint` 锁定 pitch 仅改变 yaw）
- ✅ 修复寻路愣在原地（`hasReachedWaypoint` 改为仅水平距离判定）
- ✅ 修复有路线却显示找不到（起始/目标位置规范化 + 邻居检测逻辑重写）

### 🔧 技术改进
- A* 寻路 `openSet.remove()` 从 O(n) 优化为惰性删除 O(1)
- 移除寻路距离硬限制（256格），改为动态迭代上限（15,000次）+ 部分路径回退
- **BotPathfinder 完全重写**：所有节点为"站立位置"，邻居检测简化为平地/跳跃/下落三类
- 实现物品栏/末影箱完整序列化与反序列化（SNBT 格式）
- 实现药水效果完整保存与恢复
- BotActionController 新增 9 个 public getter 替代反射访问

### ⚙️ 配置变更
- 新增 `allowBotAutoJump` 布尔配置项（默认 true）
- 新增“动作设置”配置分类

### 📝 文档更新
- COMMANDS.md: 添加 goto 命令文档
- CONFIG.md: 添加 allowBotAutoJump 配置说明
- FAQ.md: 更新自动寻路 FAQ
- DEVELOPMENT.md: 添加 BotPathfinder 到项目结构

---

## [1.2.0] - 2026-06-14

### 🎨 UI 全面重写
- **配置界面全新布局**
  - 全屏面板布局，边距缩小至 8px，内容区域更大
  - 移除顶部标题栏，Tab 栏紧贴面板顶部
  - 4 个 Tab 页：基础、战斗、生存、高级（原 5 页合并优化）
  - Section 卡片分组系统，带圆角背景和边框
  - 小号缩放字体（0.75x），整体更紧凑精致

- **组件重写**
  - `ModernSlider`：完全重写拖动机制，新增 `forceDrag()` API，新增已填充轨道视觉
  - `ModernCheckbox`：继承 `AbstractWidget`，彻底消除反射调用
  - `SectionCard`：全新卡片容器组件，支持圆角和标题
  - `UI`：工具类，支持缩放文字绘制
  - `DesignTokens`：统一设计令牌系统

- **关于界面重写**
  - 匹配配置面板风格（深色面板 + 分隔线 + 缩放字体）
  - 分层展示：模组名 → 版本/作者/许可证 → 描述 → 联系方式

- **快捷键配置重写**
  - 支持组合键捕获（Ctrl/Shift/Alt + 主键，minihud 风格）
  - 实时捕获模式：点击按钮后按下新快捷键
  - 重置按钮恢复默认 B 键

### ✨ 新功能
- **`/bot use` 模拟右键点击**
  - 优先与实体交互（`entity.interact`）
  - 方块交互：放置方块、打开箱子等（`useItemOn`）
  - 兜底使用物品：拉弓、吃食物等（`useItem`）
  - 添加挥手动画（`swing`）

### 🐛 Bug 修复
- ✅ 修复滑动条无法流畅拖动（Screen 事件路由导致拖动中断）
- ✅ 修复 `/bot use` 只能使用物品无法放置方块
- ✅ 修复假人右键交互缺少挥手动画
- ✅ 修复二级菜单返回后 Tab 状态不同步（改为 static 保持选中）
- ✅ 修复 ModernCheckbox 使用反射设置 selected 的脆弱实现
- ✅ 修复 ModernSlider 文字 Y-12 渲染与上方配置项重叠
- ✅ 修复 ResetButton 默认值错误（4 处：attackReach=3.0, creativeReach=5.0, auraRange=3.0, mountBots=false）
- ✅ 修复 `deleteBot` 先删数据再移除票据导致区块票据无法清理
- ✅ 修复 `reloadConfig` 未实际重新加载配置文件
- ✅ 修复 `fetchSkinFromMojang` 资源泄漏（HttpConnection 未关闭）
- ✅ 修复 BotManager 使用非线程安全 HashMap 的并发风险
- ✅ 修复客户端 `SKIN_FOLDER` 硬编码导致找不到皮肤文件

### 🔧 技术改进
- `BotManager` 改用 `ConcurrentHashMap` 提升线程安全性
- `BotActionController.performKillAura` 优化：移除死代码 `getNearestEntity`
- `BotPersistenceManager` 全面替换 `System.out/err` 为 LOGGER
- `ModConfig.reload()` 新增配置重新加载方法
- `MyBotModClient` 新增组合键检测系统（GLFW 按键状态精确匹配）

### 📝 文档更新
- 更新所有文档以反映 v1.2.0 修改
- 更新版本号至 v1.2.0

---

## [1.1.1a] - 2026-05-05

### 🔄 重大重构
- **假人驻留系统全面重构**
  - 参考 GugleCarpetAddition 的实现方式
  - 使用 Minecraft 的 SavedData 系统替代独立 JSON 文件
  - 数据保存在世界文件夹的 `data` 目录中
  - 每个世界独立存储，彻底解决同名存档冲突问题
  - 改为在玩家加入时触发加载（而非服务器启动时）
  - 支持单人存档和多人服务器
  - 改进的日志系统，添加 `[假人模组]` 前缀

- **滑块组件完全重写**
  - 独立的拖拽状态管理（`isDragging`）
  - 严格的边界检查，防止滑块互相干扰
  - 修复拖拽事件处理，添加按钮类型检查
  - 焦点管理系统，键盘事件只响应获得焦点的滑块
  - 精确的碰撞检测（`clicked()` 和 `isMouseOver()`）
  - 完全消除滑块联动和无法连续拖动的问题

### 🎨 UI 界面优化
- **简化配置界面**
  - 删除所有黄色分组标题（`§e§l标题`）
  - 删除所有灰色说明文本（`§7说明...`）
  - 只保留配置项本身（复选框、滑块、按钮）
  - 界面更加简洁清晰，一目了然
  - 优化组件间距，彻底解决文字重叠问题

- **配置页面更新**
  - GeneralPage：只保留 2 个复选框
  - AttackPage：只保留 2 个滑块 + 2 个配置项
  - SurvivalPage：只保留 1 个滑块 + 4 个复选框
  - MountPage：只保留 1 个复选框 + 1 个按钮
  - AdvancedPage：只保留 2 个复选框 + 2 个按钮

- **新增重置按钮系统**
  - 为每个配置项添加单独的重置按钮（显示为 "↺"）
  - 移除总的"重置为默认"按钮
  - 完成按钮居中显示
  - 重置按钮位于每个配置项右侧

### 🐛 Bug 修复
- ✅ 修复假人驻留功能在同名存档中的冲突问题
- ✅ 修复假人驻留功能在单人存档中不生效的问题（改为玩家加入时加载）
- ✅ 修复滑块互相干扰和联动的问题
- ✅ 修复滑块无法连续拖动的问题（添加按钮类型检查）
- ✅ 修复配置界面文字重叠和溢出问题
- ✅ 修复配置界面完成按钮失灵的问题（调整事件处理顺序）
- ✅ 修复关于页面 GitHub 按钮无法跳转的问题

### 🔧 技术改进
- 使用 `SavedData` 系统进行数据持久化
- 监听玩家加入事件（`ServerPlayConnectionEvents.JOIN`）触发假人加载
- 延迟加载机制（延迟 2 秒）确保玩家完全加入
- 严格的组件边界检查和焦点管理
- 改进的事件处理系统（先处理父类，再处理页面组件）
- 为 ModernCheckbox 添加 `setSelected` 方法
- 更清晰的代码结构和注释

### 📝 文档更新
- 更新所有文档以反映最新修改
- 添加假人驻留系统的详细说明
- 添加滑块组件的技术细节
- 添加重置按钮系统的说明
- 更新常见问题解答

---

## [1.1.1] - 2026-05-03

### ✨ 新功能
- **UI布局重构**
  - 采用左侧边栏 + 右侧内容区域的现代化布局
  - 侧边栏宽度80px，深色背景（0xE0000000）
  - 内容区域半透明背景（0x90000000）

- **新增 SidebarButton 组件**
  - 专门的侧边栏导航按钮
  - 激活状态显示左侧绿色高亮线
  - 悬停效果和平滑过渡

### 🐛 Bug 修复
- **修复滚动功能**
  - 完整的滚动支持
  - 正确的滚动条显示
  - 内容裁剪和边界检测

### 🎨 视觉改进
- 更深的背景色，提升视觉层次
- 绿色高亮激活状态（#00FF00）
- 统一的组件尺寸（高度24px）
- 合理的间距（组件间距6px，分组间距20px）

### 🔧 技术改进
- 正确的鼠标事件处理
- 优化的内容裁剪逻辑
- 改进的滚动偏移计算

---

## [1.1.0] - 2026-05-02

### ✨ 新功能
- **全新现代化UI系统**
  - 类似 Sodium 的现代化风格
  - 选项卡式配置界面
  - 5个配置页面：通用、攻击设置、骑乘设置、生存设置、高级设置

- **现代化UI组件**
  - `ModernButton`：现代化按钮（半透明背景、边框、悬停效果）
  - `ModernCheckbox`：现代化复选框（绿色勾选标记）
  - `ModernSlider`：现代化滑块（数值调整）
  - `TabButton`：选项卡按钮（已在v1.1.1中被SidebarButton替代）

### 🎨 视觉改进
- 深色半透明背景
- 清晰的分组和标题
- 统一的视觉风格

---

## [1.0.0] - 2026-05-02

### ✨ 核心功能
- **假人管理系统**
  - 创建、删除和控制假人玩家
  - 支持多个假人同时存在
  - 假人数量限制配置

- **动作控制系统**
  - 移动控制（前后左右）
  - 动作控制（攻击、使用、潜行、跳跃、疾跑）
  - 视角控制（看向、旋转）
  - 物品操作（丢弃、交换主副手）

- **攻击系统**
  - 射线追踪检测目标
  - 方块破坏功能
  - 杀戮光环模式
  - 可配置的攻击距离

- **骑乘系统**
  - 骑乘实体支持
  - 白名单管理
  - 允许骑乘其他假人选项

- **皮肤系统**
  - 三级优先级：Mojang API → PNG → Base64
  - 自定义皮肤支持
  - 动态皮肤加载

- **假人驻留系统**
  - 退出世界后假人依然存在
  - 保留假人状态选项
  - 自动保存和加载

- **配置系统**
  - JSON格式配置文件
  - 图形化配置界面
  - 命令行配置管理
  - 快捷键系统

- **兼容性**
  - Carpet Mod 兼容模式
  - 自动检测并避免冲突

### 📝 文档
- 完整的命令参考文档
- 配置指南
- 皮肤系统文档
- 开发文档
- 常见问题解答

### 🔧 技术实现
- 基于 Fabric 1.20.1
- Mixin 注入系统
- 完整的测试套件
- 模块化架构设计

---

## 版本说明

- **主版本号**：重大功能更新或架构变更
- **次版本号**：新功能添加
- **修订版本号**：Bug修复和小改进
- **字母后缀**：紧急修复或小补丁（如 1.1.1a）

---

## 已知问题

### 已修复
- ✅ 假人无法移动
- ✅ 假人不会跳跃
- ✅ 疾跑速度无变化
- ✅ 攻击身后目标
- ✅ 缺少挖掘功能
- ✅ 皮肤文件夹找不到
- ✅ 配置界面文字重叠（v1.1.1a）
- ✅ 滑块互相干扰和联动问题（v1.1.1a）
- ✅ 滑块无法连续拖动问题（v1.1.1a）
- ✅ 配置界面完成按钮失灵（v1.1.1a）
- ✅ 假人驻留功能同名存档冲突（v1.1.1a）
- ✅ 假人驻留功能单人存档不生效（v1.1.1a）
- ✅ 关于页面 GitHub 按钮无法跳转（v1.1.1a）
- ✅ 滑动条无法流畅拖动（v1.2.0）
- ✅ `/bot use` 无法放置方块（v1.2.0）
- ✅ 二级菜单返回后 Tab 状态不同步（v1.2.0）
- ✅ 假人右键交互缺少挥手动画（v1.2.0）
- ✅ ModernCheckbox 反射漏洞（v1.2.0）
- ✅ 配置界面文字重叠和重叠问题（v1.2.0）
- ✅ ResetButton 默认值错误 4 处（v1.2.0）
- ✅ deleteBot 区块票据泄漏（v1.2.0）
- ✅ Mojang API 资源泄漏（v1.2.0）
- ✅ 客户端皮肤文件夹找不到（v1.2.0）

### 待实现
- ⏳ 对于假人的GUI 控制界面
- ⏳ 更多动作（打开箱子、交易等）
- ⏳ Scarpet 集成

---

**注意**：本更新日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。
